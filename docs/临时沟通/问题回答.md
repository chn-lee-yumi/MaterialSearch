1. 部署环境
   1. 正在准备专属服务器，打算使用linux，本项目未来也会部署在服务器上，当下现在我的电脑上进行测试
   2. 公司总计有20人左右，同时使用的可能最多有10人
2. NAS具体情况
   1. 是群辉的NAS
   2. 应该可以吧，群辉应该支持，但是我不太会弄
   3. 不太清楚，可能可以，不过尽量不要增加复杂度
3. 使用模式
   1. 永久库会是集中扫描素材+零散归档项目临时文件这样的模式
   2. 项目文件不太会有多人同时上传，这里澄清一下，我不希望图片或者文件路径发生变化，我只希望录入数据库可以进行索引，文件本身的路径不要发生变化。我理解即使同时上传，向量化也需要排队，只要按队列进行，是不是就不会出现一同写入数据库的情况？
4. 技术能力
   1. 没有，我们团队也没有人有编程能力，主要使用AI AGENT，比如你来进行代码操作，只要你会维护，就没问题，未来会有更强大的大模型，问题就小了
   2. 倾向于开箱即用的方案，但是需要有未来可扩展的空间，比如使用sqlite，未来是否方便转换进更专业的数据库
5. 补充
   1. 其实对于项目临时文件，不太会进行跨项目搜索，就是说我如果想索引本项目的文件，基本上会指定本项目，不会同时搜索其他项目的文件，如果是多库的话，在web端或者使用agent搜索的项目内文件时，只会指定某项目数据库进行搜索。问题是是否可以方便的把项目临时文件归档到永久文件中
   2. 继续讨论，还有什么不清晰的地方吗
